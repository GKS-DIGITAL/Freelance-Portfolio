import './globals.css';

export const metadata = { title: 'GKS Digital — Websites that grow local businesses', description: 'Premium websites for ambitious local businesses.' };

export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
